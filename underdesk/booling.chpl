use ssr;
var x: [1..15] real  = [1.0, 2.0, 3.0, NAN, NAN, NAN, 7.0, 8.0, 9.0, 10.0, NAN, NAN, 13.0, 14.0, 15.0];
var b = isnan(x);
var db = diff(b);
var ibeg = whereval(1,db) + 1;
var iend = whereval(-1,db) + 2;
writeln(x);
writeln(b);
writeln(db);
writeln(ibeg);
writeln(iend);
